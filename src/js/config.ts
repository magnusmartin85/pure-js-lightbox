import { images } from "./images";

const config = {
  images,
  imageSlider: {
    showImageCounter: true,
    showImageDescription: true,
    showImageSource: true,
    showImageTitle: true,
  },
};

export { config };
